{
    "name": "asteroidtyping",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "tsc && mkdir -p dist && cp src/index.html dist/index.html && cp src/style.css dist/style.css && cp -r src/assets dist/assets"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "dependencies": {
        "repo-boot": "^0.0.8"
    },
    "devDependencies": {
        "typescript": "^6.0.3"
    }
}
